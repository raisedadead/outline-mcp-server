services:
  outline-wiki-mcp:
    build: .
    ports:
      - "${PORT:-9999}:9999"
    environment:
      - OUTLINE_BASE_URL=${OUTLINE_BASE_URL}
      - OUTLINE_API_KEY=${OUTLINE_API_KEY}
      - MCP_TRANSPORT=http
      - PORT=9999
    restart: unless-stopped
